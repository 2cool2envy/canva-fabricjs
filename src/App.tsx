import React from 'react';
import CanvasPage from './CanvasPage';  // same folder, no extension needed

const App: React.FC = () => {
  return (
    <div>
      <CanvasPage />
    </div>
  );
};

export default App;